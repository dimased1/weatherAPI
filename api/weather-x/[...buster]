// pages/api/weather-x/[...buster].js
// Этот файл ловит ВСЁ, что приходит на /api/weather-x/что-угодно

export const config = {
  api: {
    externalResolver: true,
  },
};

// Просто вызываем твой старый рабочий weather.js
import handler from "../weather.js";

export default handler;
