name = "edinburgh-forecast"
main = "src/index.js"                 # ← ЭТО ОБЯЗАТЕЛЬНО было пропущено!
compatibility_date = "2025-11-26"

# Твоё KV-пространство (ID берёшь из dashboard → Workers & Pages → KV → твоё namespace)
[[kv_namespaces]]
binding = "KV"
id = "fd0c18e84a2e4744bc184916c9b84613"   # ← замени на свой настоящий ID

# Секреты (уже должны быть загружены через wrangler secret put)
# WEATHER_KEY
# OPENAI_API_KEY
